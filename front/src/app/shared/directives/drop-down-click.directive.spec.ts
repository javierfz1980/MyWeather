import { DropDownClickDirective } from './drop-down-click.directive';

describe('DropDownClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DropDownClickDirective();
    expect(directive).toBeTruthy();
  });
});
